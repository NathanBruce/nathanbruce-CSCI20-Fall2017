

create class for User{
      integer for playerscore_
      integer for player totalscore
      void rolling dice
      
      add function for continue_or_not
      
      
}



//create class for BotPlayer {
//      integer for botscore
//      integer for bot totalscore
//      void rolling dice}
//}

//function for void dice roll in class game
//    return srand();


main() 
  continue_or_no = 0;
  
  loop that will end after playerscore_ OR bot totalscore = 100
        output ask the user if they would like to continue (1 for yes or 2 for no )
        cin >> continue_or_no
        
        if (continue_or_no == 1)
        
        Run game code: //make function in class
        
            if number is 1{
                run game function
                    call roll dice twice
            }
        
            else if {number > 1 AND number <= 6
                add diceNumber to playerscore
            }
        
        else if (continue_or_no != 1)
            cout << "your score will be added"
            player totalscore = player totalscore + playerscore_;